<Home>:
    orientation: 'vertical'
    padding: dp(14)
    spacing: dp(16)
    BoxLayout:
        spacing: dp(16)
        size_hint_y: .2
        BoxWithShadow:
        BoxWithShadow:
        BoxWithShadow:

    BoxLayout:
        spacing: dp(8)
        size_hint_y: .5


    BoxLayout:
        spacing: dp(8)
        size_hint_y: .3
